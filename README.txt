This dash_app is not successfully deployed yet, 
it connects to the SQL Server that contains all the market data and presents it in dashboard


To run the app, first install all requirements, 

set up environment variables by `export DB_DRIVER=xxxxx`

then

`python create_dash_app.py`